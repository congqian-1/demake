# 阶段 0 研究结论

## 决策 1：缺失上层实体的返回形式
- 决策：缺失层级字段固定返回为 null
- 理由：保持响应结构稳定，前端与测试易处理；与现有风格一致
- 备选方案：省略字段；返回对象并带 missing 标识

## 决策 2：层级范围
- 决策：仅返回板件上层实体（包件、箱子、预包装订单、工单、优化文件、批次）
- 理由：与需求一致，避免返回下层或无关数据导致结构膨胀
- 备选方案：返回完整树结构（含同级/子级）
