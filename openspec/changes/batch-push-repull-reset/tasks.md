## 1. Push 去重与增量补推

- [x] 1.1 梳理并修改批次推送服务，使其按 `batchNum + optimizingFileName + workId` 进行去重命中与复用
- [x] 1.2 实现同一批次下新增优化文件时复用批次、插入新优化文件及其工单的逻辑
- [x] 1.3 实现同一批次同一优化文件下新增工单时只插入新工单的逻辑
- [x] 1.4 实现重复推送命中同一批次、优化文件、工单时重置工单为 `NOT_PULLED` 并刷新相关更新时间

## 2. 重拉与定时任务行为收敛

- [x] 2.1 修改 `/{workId}/repull` 接口与服务逻辑，只重置目标工单状态、重试次数和错误信息
- [x] 2.2 新增批次级重拉接口，将指定批次下全部工单重置为 `NOT_PULLED`
- [x] 2.3 调整定时任务拉取流程，统一使用 `savePrePackageDataWithOverwrite`
- [x] 2.4 修正覆盖保存删除范围，确保仅覆盖当前工单记录关联的预包装层级数据

## 3. 数据库与索引调整

- [x] 3.1 移除 `mes_work_order.work_id` 唯一索引并补充普通索引
- [x] 3.2 校验批次、优化文件、工单的数据库约束与服务层去重逻辑一致，不再依赖错误的重复新增路径
- [x] 3.3 更新部署初始化 SQL 与增量迁移 SQL，覆盖线上升级与本地初始化场景
- [x] 3.4 更新测试数据库建表脚本，保持测试环境与生产约束一致

## 4. 验证与回归

- [x] 4.1 补充/修改集成测试，覆盖重复推送重置状态、同批次增量补推、单工单重拉、批次级重拉
- [x] 4.2 补充/修改测试，覆盖定时任务统一走覆盖保存且不误删其他合法记录
- [x] 4.3 运行测试并修复失败项，确认变更满足 capability spec
- [x] 4.4 更新接口文档或 Swagger 注释中与重拉/批次重拉行为相关的说明
